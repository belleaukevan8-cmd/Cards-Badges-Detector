// Utility functions implementation

#include <stdio.h>

// Function to print a greeting message
void printGreeting() {
    printf("Hello, welcome to the Cards-Badges-Detector!\n");
}

// Function to calculate the total number of badges
int totalBadges(int badgeCount1, int badgeCount2) {
    return badgeCount1 + badgeCount2;
}